apiVersion: v1
kind: Service
metadata:
  name: consultation-service
  namespace: neuroguard
  labels:
    app: consultation-service
spec:
  selector:
    app: consultation-service
  ports:
    - name: http
      protocol: TCP
      port: 8090
      targetPort: 8090
      nodePort: 30090
  type: NodePort
