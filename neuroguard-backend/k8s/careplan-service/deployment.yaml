apiVersion: apps/v1
kind: Deployment
metadata:
  name: careplan-service
  namespace: neuroguard
  labels:
    app: careplan-service
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: careplan-service
  template:
    metadata:
      labels:
        app: careplan-service
        version: v1
    spec:
      containers:
      - name: careplan-service
        image: neuroguard/careplan-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8085
          name: http
          protocol: TCP
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        - name: SERVER_PORT
          value: "8085"
        - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
          value: "http://eureka-server.neuroguard.svc.cluster.local:8761/eureka"
        - name: SPRING_APPLICATION_NAME
          value: "careplan-service"
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:mysql://mysql.neuroguard.svc.cluster.local:3306/careplandb"
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: careplan-db-secret
              key: username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: careplan-db-secret
              key: password
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8085
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8085
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: careplan-service
  namespace: neuroguard
  labels:
    app: careplan-service
spec:
  type: ClusterIP
  ports:
  - port: 8085
    targetPort: 8085
    protocol: TCP
    name: http
  selector:
    app: careplan-service
  sessionAffinity: None