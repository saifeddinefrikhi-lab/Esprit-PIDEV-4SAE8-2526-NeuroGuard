apiVersion: v1
kind: Service
metadata:
  name: reservation-service
  namespace: neuroguard
  labels:
    app: reservation-service
spec:
  selector:
    app: reservation-service
  ports:
    - name: http
      protocol: TCP
      port: 8087
      targetPort: 8087
      nodePort: 30087
  type: NodePort
