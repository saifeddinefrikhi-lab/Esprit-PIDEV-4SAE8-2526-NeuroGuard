apiVersion: v1
kind: Service
metadata:
  name: reservation-service
  namespace: neuroguard
  labels:
    app: reservation-service
spec:
  selector:
    app: reservation-service
  ports:
    - name: http
      protocol: TCP
      port: 8094
      targetPort: 8094
      nodePort: 30094
  type: NodePort
