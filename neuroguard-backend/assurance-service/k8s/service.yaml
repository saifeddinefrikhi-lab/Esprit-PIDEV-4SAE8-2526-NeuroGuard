apiVersion: v1
kind: Service
metadata:
  name: assurance-service
  namespace: neuroguard
  labels:
    app: assurance-service
spec:
  selector:
    app: assurance-service
  ports:
    - name: http
      protocol: TCP
      port: 8091
      targetPort: 8091
      nodePort: 30091
  type: NodePort
