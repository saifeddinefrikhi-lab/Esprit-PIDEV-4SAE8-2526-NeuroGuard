apiVersion: v1
kind: Service
metadata:
  name: assurance-service
  namespace: neuroguard
  labels:
    app: assurance-service
spec:
  selector:
    app: assurance-service
  ports:
    - name: http
      protocol: TCP
      port: 8093
      targetPort: 8093
      nodePort: 30093
  type: NodePort
